version: 2

seeds:
  - name: netflix_titles
    description: "Cleaned dataset exported by main.py into dbt/seeds/netflix_titles.csv"
    columns:
      - name: title
        tests:
          - not_null
      - name: type
        tests:
          - accepted_values:
              arguments:
                values: ['Movie', 'TV Show']

models:
  - name: stg_netflix_titles
    description: "Staging model with casted types and basic normalization"
    columns:
      - name: show_id
        tests:
          - not_null
          - unique
      - name: title
        tests:
          - not_null
      - name: type
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['Movie', 'TV Show']
      - name: release_year
        tests:
          - not_null
  - name: mart_titles_by_country
    description: "Country level metrics for dashboarding"
    columns:
      - name: country
        tests:
          - not_null
          - unique
      - name: title_count
        tests:
          - not_null
      - name: movie_count
        tests:
          - not_null
      - name: tv_show_count
        tests:
          - not_null
      - name: min_release_year
        tests:
          - not_null
      - name: max_release_year
        tests:
          - not_null

